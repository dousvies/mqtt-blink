#ifndef CONSTANTS_h
#define CONSTANTS_h

const extern int led_pin;

const extern char* wifi_ssid;
const extern char* wifi_password;

const extern char* mqtt_server_address;
const extern int mqtt_server_port;
const extern char* mqtt_client_name;

const extern char* led_mqtt_topic;

#endif
